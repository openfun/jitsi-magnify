apiVersion: v1
kind: Secret
metadata:
  labels:
    app: magnify
    service: magnify
  name: "{{ magnify_secret_name }}"
  namespace: "{{ namespace_name }}"
data:
{% for k, v in MAGNIFY_VAULT.items() %}
  {{ k }}: {{ v | default('') | b64encode }}
{% endfor %}
